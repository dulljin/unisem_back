/**
 * http://usejsdoc.org/
 */
const dbConfig = {
		server : '',
		user : '',
		password : '',
		database : '',
		port: 0,
		options: {
			encrypt: true
		}
};

const secret = '';

module.exports = {dbConfig, secret};